# git-cascade

Git-native CLI for planning and applying cascade rebases across dependent branch stacks.

## Workspace

The Rust workspace is intentionally empty for now. Crates will live under `crates/` once implementation begins.

## Verify

```sh
cargo make check
```
